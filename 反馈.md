# 五合一 — 完成反馈

## 修复1：核心居中

`max-w-5xl` (1024px) + `justify-center`，左右留白均匀。

## 修复2：垂直对齐

右列顶部添加 `hidden lg:block h-[52px] mb-4` 占位，对齐左列 Navigation 高度。

## 修复3：Timeline 空状态

空事件时显示 "📅 时间线 · 暂无事件" 占位条，不消失。

## 修复4：溢出滚动

展开时 `max-h-[360px] overflow-y-auto`，内部事件超过360px可滚动，不撑破页面。

## 修复5：动画 600ms

`transitionDuration: 600ms` + `ease-out-expo`，比默认500ms更平滑。

## 运行结果

| 命令 | 结果 |
|:-----|:-----|
| `npm run build` | ✓ 2.67s |
