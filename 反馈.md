# V1.1 小修复 — 完成反馈

## ✅ 完成状态

### 修改

| 文件 | 说明 |
|:-----|:-----|
| `src/components/calendar/GridCell.tsx` | L133: `phaseIcon` 根据 viewMode 选择正确的月相源 |

```tsx
// Before:
const phaseIcon = MOON_PHASE_ICONS[lunarPrimary.phase] || ''

// After:
const phaseSource = viewMode === 'lunar-secondary' ? lunarSecondary.phase : lunarPrimary.phase
const phaseIcon = MOON_PHASE_ICONS[phaseSource] || ''
```

副月历视图下现在显示副月实际的月相图标，而非错误复用主月月相。

### 运行结果

| 命令 | 结果 |
|:-----|:-----|
| `npm run typecheck` | 0 errors |
| `npm run build` | ✓ 2.23s |

### Git 提交

项目根目录未初始化 git 仓库（`is a git repository: false`）。如需提交推送，请先：

```bash
cd c:\0000\Github\calendar
git init
git add .
git commit -m "V1.1: 副月历引擎 + 三模式视图切换 + 年份跳转导航"
git remote add origin <your-repo-url>
git push -u origin main
```
