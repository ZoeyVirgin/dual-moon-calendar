# 两合一 — 完成反馈

## 修复1：自定义滚动条

月份下拉列表：Firefox `scrollbarWidth: 'thin'` + webkit 伪元素（4px宽、border-light 色 thumb、透明 track）。

## 修复2：跳转面板单行布局

`flex-col gap-2`（两行）→ `flex items-center gap-1`（单行）：
年框(16px) → 年 → 月下拉(16px) → 跳转按钮，水平紧凑排列。

## 运行结果

| 命令 | 结果 |
|:-----|:-----|
| `npm run typecheck` | 0 errors |
| `npm run build` | ✓ 2.44s |
