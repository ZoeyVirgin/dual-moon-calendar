# vercel.json 精简 — 完成

```json
{ "buildCommand": "npm run build", "outputDirectory": "dist" }
```

Vercel 从 vite.config.ts 自动检测 Vite 预设 → 自动 SPA fallback + 自动扫描 api/ 目录部署 Serverless Functions。

| 命令 | 结果 |
|:-----|:-----|
| `npm run build` | ✓ 2.30s |
