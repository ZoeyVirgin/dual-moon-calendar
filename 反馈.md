# 删除 vercel.json — 纯自动检测

## 操作

1. 删除 `vercel.json`
2. `package.json` 添加 `"engines": { "node": "20.x" }`
3. Dashboard → Settings → Framework Preset → **Vite** → Save → Redeploy

## 验证

| 命令 | 结果 |
|:-----|:-----|
| `npm run build` | ✓ 2.18s |
