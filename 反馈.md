# 三合一 — 完成反馈

## 修复1：双月合朔/冲日引擎重写

旧：37.104天周期 → 每37天"双月合朔"（错误）
新：两月月龄直接比较 → 同时趋近朔/望时判定 S/A，±3天容差，≈812天一次合朔

## 修复2：三栏居中布局

左288(详情+节日) | 中420(导航+日历+时间线) | 右288(留白)
`justify-center` + `order-*` 实现桌面端视觉居中对称。

## 修复3：日历背景区分

CalendarGrid 容器 `bg-[var(--bg-primary)]`，GridCell 当月格子同底色，
底板 `--bg-page` 灰色 → 日历白色卡片自然抬升。

## 运行结果

| 命令 | 结果 |
|:-----|:-----|
| `npm run typecheck` | 0 errors |
| `npm run build` | ✓ 2.54s |
