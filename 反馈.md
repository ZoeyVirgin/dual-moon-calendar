# 年份1200边界崩溃修复 + 软上限5000 — 完成反馈

## 修复1：generateMonthDates 边界保护

`CalendarGrid.tsx` — 后置填充时检查 `nextAbs > maxAbs`，超出时 `break` 停止填充，不再崩溃。

## 修复2：MAX_YEAR 5000 + engine 软限制

| 文件 | 改动 |
|:-----|:-----|
| `constants.ts` | `MAX_YEAR: 1200` → `5000` |
| `solar.ts` | `getYearStartAbs` 年份 > MAX_YEAR 仅 `console.warn`，不抛错 |
| `calendar.ts` | `getMaxAbsoluteDay` 改为通用 `getYearStartAbs(lastYear+1)-1`；`getCalendarDate` 仅拒负数 |
| `calendar.ts` | `computeYearStartAbs` 累积缓存，O(1) 命中，5000 年无卡顿 |

## 修复3：Navigation 去除硬边界

- `isAtMonthEnd` / `isAtYearEnd` 已删除
- 下一月/下一年按钮不再 disabled
- `goToYearMonth` clamp 更新为 5000
- Hint 文案："年份限制 0-5000，已自动调整"

## 修复4：跳转UI风格统一

- 年份 Input 使用现有组件（`type="number"`, `size="sm"`）
- 月份 select: `bg-[var(--bg-primary)] text-[var(--text-primary)]`
- 面板背景跟随主题

## 运行结果

| 命令 | 结果 |
|:-----|:-----|
| `npm run typecheck` | 0 errors |
| `npm run test` | 242 passed (0.91s) |
| `npm run build` | ✓ 2.17s |
