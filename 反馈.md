# 换回 Vite 预设 — 测试标准格式 Serverless Functions

## 关键变化

- vercel.json 已删除（Vite 预设完全自动检测）
- api/ 文件已全部改为标准 Vercel 格式（`export default function handler(req, res)`）
- 不再是 Next.js App Router 格式（`export function GET()`）

## Dashboard 操作

1. Framework Preset → **Vite** → Save
2. Deployments → Redeploy → **勾选 Clear Build Cache**

## 验证

| 命令 | 结果 |
|:-----|:-----|
| `npm run build` | ✓ 2.40s |

部署后访问 `/api/ping` → 期望 `{"ok":true}`

## 结果

待部署验证。
