# V1.3 修复 — 深色模式覆盖 + 事件列表更新 + 过渡动画

## 修复1：硬编码 bg-white → CSS 变量

| 文件 | 修改 |
|:-----|:-----|
| `MainLayout.tsx` L19-20 | `backdrop-blur-sm bg-white/90` → `bg-[var(--bg-primary)]/90 backdrop-blur-sm` |
| `DetailPanel.tsx` L80 | `bg-white` → `bg-[var(--bg-primary)]` |

## 修复2：DetailPanel 事件列表实时更新

订阅 `eventsRaw`（整个事件数组），使其在 addEvent/deleteEvent 后自动触发重算。

## 修复3：主题切换过渡动画

```css
html { transition: background-color 0.3s ease; }
* { transition: background-color 0.3s ease, color 0.3s ease, border-color 0.3s ease; }
```

## 验证

| 命令 | 结果 |
|:-----|:-----|
| `npm run build` | ✓ 2.27s |
