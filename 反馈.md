# Toast 动画 + ROADMAP.md 更新 — 完成反馈

## Toast 淡入淡出

- `tailwind.config.ts`: 新增 `fadeOut` keyframe + `fade-out` animation (200ms ease-in)
- `Toast.tsx`: 出现 `animate-fade-in` → 2.5s后 `animate-fade-out` → 200ms后 `onClose` 移除 DOM
- 点击 Toast 也触发退出动画

## ROADMAP.md

已全面重写，反映当前实际代码状态。包含：
- 已完成清单（历法引擎/UI/事件/权限/主题/布局/CI/性能）
- 待完成（副月历闰月/Playwright E2E，按优先级）
- 已舍弃项及原因

## 运行结果

| 命令 | 结果 |
|:-----|:-----|
| `npm run build` | ✓ 2.20s |
