# 三合一 — 完成反馈

## 修复1：年份 Input 去原生箭头

`type="number"` → `type="text" inputMode="numeric" pattern="[0-9]*"` — 无 spinner，移动端弹出数字键盘。

## 修复2：月份选择器 → 下拉 popover

3×4 按钮网格 → 紧凑下拉按钮（显示 "X月 ▾"），点击弹出 12 行垂直列表，选中靛蓝高亮。

## 修复3：边界 hint 反馈

4 个导航按钮终点点击时显示琥珀色提示：
- `handlePrevYear`/(0年)→"已到达纪元起点"
- `handleNextYear`/(≥5000)→"已到达时间上限"
- `handlePrevMonth`/(0/1)→"已到达纪元起点"
- `handleNextMonth`/(≥5000/12)→"已到达时间上限"

## 运行结果

| 命令 | 结果 |
|:-----|:-----|
| `npm run typecheck` | 0 errors |
| `npm run build` | ✓ 2.48s |
