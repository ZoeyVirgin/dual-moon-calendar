# api/ 调试 — /api/ping 测试端点

## 新增

`api/ping/route.ts` — 零依赖 GET 端点，返回 `{ ok: true, time: <timestamp> }`

## 操作

- 已删除 `.vercel` 本地缓存目录
- Dashboard Framework Preset → Vite

## 部署后验证

1. 浏览器直接访问 `https://<域名>/api/ping`
2. 返回 `{"ok":true}` → api/ 目录正常部署，问题在具体路由（@vercel/postgres 导入）
3. 返回 404 → api/ 目录根本没被部署

## 构建

| 命令 | 结果 |
|:-----|:-----|
| `npm run build` | ✓ 2.68s |
