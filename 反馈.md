# 三合一修复 — 完成反馈

## 修复1：Input / Navigation select 深色模式

| 文件 | 改动 |
|:-----|:-----|
| `Input.tsx` L82 | `bg-white` → `bg-[var(--bg-primary)]` |
| `Navigation.tsx` L104 | select 元素 `bg-white` → `bg-[var(--bg-primary)]` |

## 修复2：越界 hint 可见

`Navigation.tsx` L65：父 div 添加 `relative`，使 `absolute` 定位的 hint 相对于导航栏居中显示。

## 修复3（核心）：getYearStartAbs 缓存

`src/engine/solar.ts` — `computeYearStartAbs` 改为累积缓存：

```typescript
const _yearStartCache: number[] = [0]

function computeYearStartAbs(year: number): number {
  if (year < _yearStartCache.length) return _yearStartCache[year]
  for (let y = _yearStartCache.length; y <= year; y++) {
    const prev = _yearStartCache[y - 1]
    const days = isLeapYear(y - 1) ? ... : ...
    _yearStartCache.push(prev + days)
  }
  return _yearStartCache[year]
}
```

首次请求 year=N 时计算 0..N，后续 O(1) 命中，1200 年完全无卡顿。

## 运行结果

| 命令 | 结果 |
|:-----|:-----|
| `npm run typecheck` | 0 errors |
| `npm run test` | 242 passed (0.97s) |
| `npm run build` | ✓ 2.34s |
