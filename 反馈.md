# 事件导出 — JSON + CSV 完成反馈

## ✅ 完成状态

### 新增

`src/components/events/EventExport.tsx` — 下拉导出按钮（仅作者可见）

### 修改

`src/components/events/EventList.tsx` — 在"创建新事件"按钮同行右侧添加 `<EventExport />`

### 功能

- 仅作者模式下可见（游客不显示）
- 点击"导出" → 弹出下拉菜单：导出 JSON / 导出 CSV
- JSON：格式化的 JSON 文件（含 id/title/description/type/date/abs/category/createdAt）
- CSV：BOM UTF-8（Excel 直接打开不乱码），中文列名
- 文件名：`双月合历-事件-YYYYMMDD.json` / `.csv`

### 运行结果

| 命令 | 结果 |
|:-----|:-----|
| `npm run typecheck` | 0 errors |
| `npm run build` | ✓ 2.22s |
