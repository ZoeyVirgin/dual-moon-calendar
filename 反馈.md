# 两个修复 — 完成反馈

## 修复1：navigateMonth 边界同步 MAX_YEAR

`useCalendarStore.ts` L48：`1200` → `5000`

5000年12月时 ▶ 不再推进，正确停在边界。

## 修复2：月份选择器改为自定义按钮组

`Navigation.tsx`：原生 `<select>` → 3列×4行 Indigo 按钮网格

- 选中态：`bg-[var(--accent-500)] text-white`
- 未选中：`bg-[var(--bg-primary)]` + hover 效果
- 年份 input 隐藏浏览器原生 spinner 箭头

## 运行结果

| 命令 | 结果 |
|:-----|:-----|
| `npm run typecheck` | 0 errors |
| `npm run build` | ✓ 2.74s |
