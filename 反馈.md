# 三合一修复 — 完成反馈

## 修复清单

| # | 修复项 | 文件 | 改动 |
|:--|:-------|:-----|:-----|
| 1 | EventModal 深色模式 | `EventModal.tsx` | 4处 `bg-white` → `bg-[var(--bg-primary)]`；`bg-[var(--accent-50)]` → `bg-[var(--bg-active)]` |
| 2 | 事件导出按钮始终可见 | `EventExport.tsx` | 移除空列表时的 `return null`，改为点击时 `alert("暂无事件可导出")` |
| 3 | 年份边界校验 + 提示 | `useCalendarStore.ts` | `navigateMonth` 边界 guard；`goToYearMonth` clamp |
| 3b | 越界友好提示 | `Navigation.tsx` | 输入越界时显示"年份限制 0-1200，已自动调整"（2.5s 自动消失） |
| 4 | 月切换过渡动画 | `App.tsx` | `key={`${year}-${month}-${viewMode}`}` + `animate-fade-in` |

## 运行结果

| 命令 | 结果 |
|:-----|:-----|
| `npm run typecheck` | 0 errors |
| `npm run build` | ✓ 2.15s |
