# vercel.json — Framework Other + @vercel/node@3

## 修改

```json
{
  "functions": {
    "api/**/*.ts": { "runtime": "@vercel/node@3" }
  }
}
```

关键差异：`@vercel/node@3`（正确格式）替代 `nodejs20.x`（不被 Other 预设识别）。

Dashboard 同步：Settings → Framework Preset → **Other** → Save → Redeploy。
