# 四合一 — 完成反馈

## 修复1：导航回归全宽

Navigation 从中间 column 内移回顶栏 `max-w-7xl`，两列从相同 y 起点对齐。

## 修复2：导出下拉裁剪修复

DetailPanel `overflow-hidden` 仅折叠时生效，展开时 `overflow-visible`（默认），EventExport 下拉不再被裁切。

## 修复3：整体放大

- GridCell: `min-h-[64px]/sm:min-h-[84px]`（原60/80）
- 主容器: `p-3 sm:p-5`（原p-4）
- DetailPanel: `max-h-[800px]`（原600px）

## 修复4：nowrap + 紧凑间距

DetailPanel A区：`gap-x-2` + `whitespace-nowrap`，长文本不换行。

## 运行结果

| 命令 | 结果 |
|:-----|:-----|
| `npm run build` | ✓ 2.47s |
