# V1.3 — 浅色/深色主题切换 完成反馈

## ✅ 完成状态：全部通过

### 创建/修改的文件

| 文件 | 动作 | 说明 |
|:-----|:-----|:-----|
| `src/styles/theme.css` | **更新** | 添加 `[data-theme='dark']` 覆盖 14 个 CSS 变量 |
| `src/store/useCalendarStore.ts` | **更新** | 新增 `theme` 状态 + `toggleTheme()` + localStorage 持久化 |
| `src/components/layout/ThemeToggle.tsx` | **新建** | Sun/Moon 图标切换按钮 |
| `src/App.tsx` | **更新** | useEffect 初始化 data-theme + HeaderActions 添加 ThemeToggle |

### 运行结果

| 命令 | 结果 |
|:-----|:-----|
| `npm run typecheck` | 0 errors |
| `npm run lint` | 0 errors, 0 warnings |
| `npm run test` | 242 passed |
| `npm run build` | ✓ 2.33s |

### 深色主题色值

| 变量 | 浅色 | 深色 |
|:-----|:-----|:-----|
| --bg-primary | #FFFFFF | #0F1119 |
| --bg-secondary | #F9FAFB | #1A1D2B |
| --bg-tertiary | #F3F4F6 | #252836 |
| --text-primary | #111827 | #EBECF0 |
| --text-secondary | #6B7280 | #9BA1B0 |
| --border-light | #E5E7EB | #2D3143 |

### 功能

- Header 显示 Sun/Moon 图标按钮
- 点击切换 → `data-theme` 属性即时更新 + localStorage 存储偏好
- 刷新后偏好保留
